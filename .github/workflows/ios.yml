name: Build (iOS)

on:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    name: Build and Test (iOS)
    runs-on: macos-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          distribution: zulu
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6.1.0

      - name: Build and test iOS targets + XCFramework
        # Derive the XCFramework task from build.gradle.kts so this
        # workflow stays portable across kotlinmania repos.
        run: |
          XCF_NAME=$(grep -oE 'XCFramework\("[^"]+"\)' build.gradle.kts | head -1 | sed 's/.*"\(.*\)".*/\1/')
          if [ -z "$XCF_NAME" ]; then
            echo "::error::No XCFramework(\"<Name>\") declaration found in build.gradle.kts"
            exit 1
          fi
          echo "XCFramework name: $XCF_NAME -> task: assemble${XCF_NAME}XCFramework"
          ./gradlew \
            compileKotlinIosArm64 \
            compileKotlinIosSimulatorArm64 \
            iosSimulatorArm64Test \
            "assemble${XCF_NAME}XCFramework" \
            -PenableIosSimulatorTests=true \
            --no-configuration-cache
